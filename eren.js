console.log("hellow world");
console.log(32);
// this is number type variable 
var price = 100;
console.log(price );
// java script varibles Here are string variables those maning of words 
var name = "eren";
console.log( typeof name);
// and these is boolean type variable
var isTrue = true;
console.log( typeof isTrue);
// and there are many types variable are there in  
 
//variavle name and then type .toLOwercase then all the text will be lower cases there are many modules and .toUpperCase command all Upper case
var newName = "the Cew name"
console.log(newName.toLowerCase());
// .indexOf will find the var possision it will start from 0 ; if it show -1 then the variable is not found.
var hackerThron =" pretiere then code";
console.log(hackerThron.indexOf("code"));
// .spilt will devide the var it will add a coma between each word
var erenYeager ="the attack titan ";
console.log(erenYeager.split(" "));
// numeriacl variable "intiger and float" int and float will never be able to +-*/ so we need to purefy those number  
var number1 =10;
var number2 = 10.5;
number2 = parseFloat(number2);
number1 = parseInt(number1);
result = (number1 + number2)	
console.log(result )
//java script operators are + - * / %-modulas(vhagsesh) {} () ** ++ -- here are ++ meaning that it will add 1 to the variable
var numberX =24;
var numberY =47;
var total = numberX + numberY;	
console.log(total);

// if you put any number in dubble quotes it will be a string
var numberX =24;
var numberY ="47";
var total = numberX + numberY;	
console.log(total); //the result will be show 2447 not 71 so it will be a string
//another thing that is if you put ++ on your code it will be add 1 to the variable
var nameOfVar = 24;
nameOfVar = nameOfVar + 1; //you can use this method but also you can use nameOfVar++ then it will be add 1 to the variable
console.log(nameOfVar);
//an other things that the use of % in java script 
var Gname = 10;
var oldName = 25;
var total = Gname % oldName; //the result will be show 5 it means that 10 is not dividable by 25 
console.log(total);

//mathmatical oparator in javasceript here are 
//* absulute it means that it will be convert any float number into integer 
var numberX = 5.2637846;
var result = math.abs(numberX);
console.log(result) // now it will be show the number 5 
// round it means that it will round the number if the number value is less than 0.5 it will be show 0 and it will be avobe .5 than it will be show 1 
 var numberO =7.779;
 var result =math.round(numberO);
 console.log(result); // now it will be show the number 8 
 // ceil it means that it will round the number if the number value is less than 0.5 it will be show 0 and it will be avobe .5 than it will be show 1 
 var numberX =7.779;
 var result =math.ceil(numberX);
 console.log(result); // now it will be show the number 8 
 // floor it means that it will round the number if the number value is less than 0.5 it will be show 0 and it will be avobe .5 than it will be show 1
 // var numberX =7.779;
 var result =math.floor(numberX);
 console.log(result); // now it will be show the number 7
 // max it means that it will show the max number
 var numberX =7.779;
 var result =math.max(numberX);
 console.log(result); // now it will be show the number 8
 // min it means that it will show the min number
 var numberX =7.779;
 var result =math.min(numberX);
 console.log(result); // now it will be show the number 7   
//random it means that it will show the random number
var numberX =7.779;
var result =math.random(numberX);
console.log(result); // now it will be show the number 0.823897  or nay random nunber it useually use in lottary games